/* eslint-disable react/prop-types */

const Heading = ({ title }) => {
    return (
        <div className="text-4xl font-bold text-center w-fit mx-auto">
            {title}
        </div>
    );
};

export default Heading;